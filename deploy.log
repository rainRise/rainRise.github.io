步骤01 - 环境检查与安装：
  a. 打开 PowerShell 或 CMD，运行 `node --version`、`npm --version`、`git --version`，确认 Node.js (>=18) 与 Git 已就绪；若命令找不到，使用 `winget install OpenJS.NodeJS.LTS` 与 `winget install Git.Git` 安装后重新打开终端。
  b. Node.js 自带 npm，如此前安装的是较旧版本，请先卸载旧 Node 再装 LTS，避免编译失败。

步骤02 - 安装 pnpm（项目必须使用）：
  a. 在同一终端执行 `npm install -g pnpm`，安装完成将自动把 pnpm 写入 PATH。
  b. 运行 `pnpm --version` 确认命令可用；若提示权限不足，请以管理员身份重新执行或在命令前增加 `Set-ExecutionPolicy RemoteSigned -Scope CurrentUser`。

步骤03 - 获取并进入项目：
  a. 将项目源码放在任意目录（此示例为 `C:\Users\19160\OneDrive\Desktop\CodeStudy\Mizuki-main-taskDemo`）。
  b. 在终端执行 `cd C:\Users\19160\OneDrive\Desktop\CodeStudy\Mizuki-main-taskDemo` 进入项目根目录，后续命令均在此目录运行。

步骤04 - 安装依赖：
  a. 首次运行 `pnpm install`，pnpm 会读取 `package.json` 并把依赖装到 `node_modules`；安装期间如出现 `Only pnpm is allowed`，说明你误用了 npm/yarn，需要重新执行 pnpm 命令。
  b. 安装完毕后，可在 `pnpm-lock.yaml` 看到锁定版本；若下载被中断，可以删除 `node_modules` 与 `pnpm-lock.yaml` 后重新安装。

步骤05 - 核对站点配置：
  a. 使用任意编辑器打开 `src/config.ts`，修改站点标题、副标题、`siteURL` 等字段，使其符合自己的域名/仓库地址（部署 GitHub Pages 时必须形如 `https://用户名.github.io/仓库名/`）。
  b. 如需启用评论、音乐、Live2D 等功能，也在此文件中填写自己的 ID 或资源地址；保存文件后再运行下面的预览/构建命令。

步骤06 - 启动本地开发服务器（调试阶段）：
  a. 运行 `pnpm dev`，终端显示 `Local: http://localhost:4321` 代表启动成功。
  b. 打开浏览器访问该地址，检查页面、语言、图片、评论是否符合预期；修改内容后浏览器会自动热更新。
  c. 若端口被占用，可通过 `pnpm dev -- --port 4000` 改为其他端口。

步骤07 - 生产构建：
  a. 在确认内容无误后执行 `pnpm build`，Astro 会将静态站点输出到 `dist/` 目录；终端若提示类型错误或配置缺失，请根据报错信息调整代码再重新构建。
  b. 可选：运行 `pnpm preview`，该命令读取 `dist` 并启动只读服务器用于终检，终端会给出访问地址。

步骤08 - 初始化 Git 并推送（GitHub Pages 示例）：
  a. 若尚未建仓库，先在 GitHub 创建一个空仓库（例如 `mizuki-blog`）。
  b. 回到项目根目录执行：
     `git init`
     `git add .`
     `git commit -m "Initial blog setup"`
     `git branch -M main`
     `git remote add origin https://github.com/你的用户名/仓库名.git`
     `git push -u origin main`
  c. 推送成功后，GitHub 会显示 main 分支的代码。

步骤09 - 启用 GitHub Pages 自动部署：
  a. 仓库中已包含 `.github/workflows/CI.yml`，该文件会在你 push 后自动：安装依赖 → `pnpm build` → 把 `dist` 发布到 `gh-pages` 分支。
  b. 在 GitHub 仓库页面进入 Settings → Pages，Source 选择 `Deploy from a branch`，Branch 选 `gh-pages` 与 `/ (root)`，点击保存。
  c. 打开 Actions 标签页等待工作流运行完成（状态为绿色 ✔️），此时访问 `https://你的用户名.github.io/仓库名/` 即可看到博客。

步骤10 - 其他部署方式（例如 Vercel/Netlify）：
  a. Vercel：登录 vercel.com → Import Project → 选择 GitHub 仓库 → Build Command 填 `pnpm build`，Output Directory 填 `dist`（默认即为 dist）→ 点击 Deploy，首次部署成功后可绑定自定义域名。
  b. Netlify：登录 app.netlify.com → Add New Site → Import from Git → 选择仓库 → Build command `pnpm build`，Publish directory `dist` → Deploy Site；完成后同样可配置自定义域名/HTTPS。

故障排查提示：若 `pnpm install` 长时间无响应，可更换网络或设置国内镜像；若 `pnpm build` 报错 `ReferenceError: window is not defined`，请检查是否在服务端代码中直接调用仅浏览器可用的 API，并加上运行环境判断。
